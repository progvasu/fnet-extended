dir: /content/sg_dataset
train:
    SCALES: [450, 500, 550, 600, 600, 650, 700, 750] # more probability to 600 #vasu?
    MAX_SIZE: 1000 # used if scale and ratio calculation, see data loaders
test:
    SCALES: [600,]
    MAX_SIZE: 1000
